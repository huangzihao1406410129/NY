spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://39.107.158.9 :3306/H3?characterEncoding=UTF-8
    username: root
    password: root
  servlet:
    multipart:
      max-request-size: 10MB
      max-file-size: 10MB
server:
  port: 8080
  servlet:
    context-path: /ks

mybatis-plus:
  mapper-locations: classpath*:/mapper/**/*.xml

